import { css } from 'styled-components';

const disableHighlight = css`
  -webkit-tap-highlight-color: transparent;
  -webkit-tap-highlight-color: rgba(0, 0, 0, 0);
`;

export default disableHighlight;
